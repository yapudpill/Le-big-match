Le schéma relationnel est essentiellement une traduction assez naturelle du diagramme ER. Pour l'élimination de l'héritage des évènements, nous avons opté pour conserver toutes les entités et faire des entités filles des entités faibles. Nous avons fait ce choix car en éliminant l'entité `Evenement`, tout ses attributs auraient été reportés dans les filles et cela aurait pû induire plus d'erreurs recopie quand les évènements qui viennent de passer sont déplaces chaque jour dans la table appropriée ; au contraire, éliminer les entités filles n'auraient pas permis de garantir que  les tables `presence` et `reponse` se réfèrent bien à des évènements passés et futurs. Pour aider à maintenir la cohérence entre les différentes tables d'évènements (un évènement est exactement dans une table fille à la fois, les évènements de la table futur sont réellement futurs...), nous avons fourni une requête qui renvoie exactement les lignes incohérentes.

Concernant les tags, nous n'avons pas créé de table pour cette entité. En effet, elle ne se compose que d'une unique clé qui est reportée dans toutes les associations auxquelles l'entité `Tag` participe, il n'aurait donc pas de sens de garder cette table inutile.

On a finalement agrémenté le schéma de deux vues qui semblent pertinentes dans le cadre de cette application : la vue `util_desc` qui fait un left join entre `utilisateur` et `description` pour pouvoir simultanément accéder aux données peronnelles d'un utilisateur et à sa description physique, et la vue `match` qui extrait de la table `avis` les couples d'utilisateurs qui se sont likés mutuellement.
